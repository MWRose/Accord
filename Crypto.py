def aes_encrypt(msg: bytes, aes_key: bytes) -> bytes:
    """
    Takes msg and encrypts it under aes_key
    """
    
    return


def aes_decrypt(msg: bytes, aes_key: bytes) -> bytes:
    """
    Takes msg and decrypts it with aes_key
    """
    return


def rsa_encrypt(msg: bytes, public_key: bytes) -> bytes:
    """
    Takes msg and encrypts it under public_key
    """
    return


def rsa_decrypt(msg: bytes, public_key: bytes) -> bytes:
    """
    Takes msg and decrypts it under private_key
    """
    return
